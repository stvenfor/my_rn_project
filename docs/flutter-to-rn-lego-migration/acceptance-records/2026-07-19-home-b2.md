# Acceptance Record — `@features/home` B2

Date: 2026-07-19  
Scope: home-b2-audit + parallel all-services-route-audit + home-dubbing-visuals + tab/states 收口  
Decision: **Accept**（配音页部分布局/文案 Degraded，见 DEGRADE 文档）

## Gaps closed

| ID | 处理 |
|----|------|
| G1 | 保留 BFUI Gallery/Invite；manifest note |
| G2 | 装饰图接线 + 组件抽取 |
| G3/G4 | manifest notes 更新 |
| G6 | refresh rejected 写 `error` |
| G8 | `RoutePath.communityTab` |

## Verification

```text
npx jest packages/features/home --no-coverage
npx eslint packages/features/home --ext .ts,.tsx
```

## Interaction script

1. HomeTab：区块顺序冒烟；Club → 社区 Tab  
2. 全部服务：BFUI 模板目录 / 邀请好友可进；无 feature import  
3. 配音首页 / 热榜：麦穗、rank badge、play badge、通知等可见  
4. 下拉刷新失败时可出现 error（mock 失败路径单测覆盖）  

## Known gaps

见 `packages/features/home/DEGRADE-home-dubbing-visuals.md`（history 图标、文案、热榜头渐变、2 列 grid 等）。
