# Acceptance Record — `@features/settings` B3

Date: 2026-07-19  
Scope: B3 checklist；Mine shell + Settings DEV 顺序 + logout  
Decision: **Accept**

## Verification

```text
npx jest packages/features/settings --no-coverage -u
```

## Checklist evidence

| 项 | 证据 |
|----|------|
| MineTab / Settings | `registerSettingsFeature`；Mine 含 QuickServices + MenuList |
| Dialog / Invoice / HTTP | Settings 列表入口 → 既有 Migrated 屏 |
| DEV 列表顺序 | Dialog→Linking→Realtime→IM；其后 Live/Friend/Pay smoke（B9） |
| 退出登录 | `logoutThunk` + `navigation.reset({ routes: [Login] })` |

## Interaction script

1. MineTab：见「常用服务」四项 + 「个人功能」网格 + 底部菜单（设置可进 Settings）  
2. Settings `__DEV__`：弹框 → 链接 → Realtime → IM →（可选）Live/Friend/Pay  
3. 登录后 Mine 点退出 → 落到 Login，且无法返回 Mine  
4. 未登录点「短视频」/「二手车」→ Login；登录后应进对应页（LoginRedirect）  

## Deferred

- 常用服务四项业务页（Flutter 亦 toast「开发中」）  
- 菜单项除「设置」外业务页（Flutter 亦 toast）  
