# Acceptance Record — A 全局门禁（政策 + 可证项）

Date: 2026-07-19  
Scope: 08 §A；M-close-gap 政策拍板  
Decision: **Accept with conditions**（lint 全仓清零 / design-token 抽检 Deferred）

## Policy (locked)

| Gate | Policy |
|------|--------|
| typecheck | 全仓零 error |
| lint | 改动路径零新增 error；全仓清零 → M2 |
| test | parity 三测 + 边界检查必绿 |
| A2 | 认 B10 + manifest 机检 |
| A3 | 壳/鉴权以既有测 + B1/B3 证据勾选；三端真机仍属 §D |

## Verification

```text
npm run agent:pre -- --slice docs/flutter-to-rn-lego-migration/plans/slices/a-gates-policy.md
npm run typecheck
npx jest src/__tests__/pageParity.test.ts src/__tests__/assetParity.test.ts src/navigation/__tests__/routeRegistration.test.ts --no-coverage
npm run agent:check-boundary
npm run agent:post
```

## Fixes this slice

- `SettingsWrappers` Mine logout：`getParent` + `CommonActions.reset` → `Login`（修 tab `reset` 类型错误，对齐 Flutter `offAllNamed`）

## Checklist evidence

| 项 | 结果 |
|----|------|
| typecheck | ✅ 全仓绿 |
| lint 全仓 | ☐ Deferred M2（基线 ~40 error）；政策：路径零新增 |
| parity 三测 | ✅ |
| boundary | ✅ |
| A2 | ✅ via B10 |
| A3 壳/登录登出 | ✅ 测 + B1/B3；真机 smoke → §D |
| design-system tokens 抽检 | ☐ Deferred（并入 C 或 M2） |

## Lint baseline (approx)

`npm run lint` → ~40 errors / ~112 warnings（多为 prettier / inline-styles）；不在本 Slice 清债。

## Deferred

- 全仓 lint 清零 Epic（M2）  
- §A1 design-token 抽检  
- §D 三端 smoke  
