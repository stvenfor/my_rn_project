# Acceptance Record — `@features/auth` B1

Date: 2026-07-19  
Scope: B1 checklist；mock session 契约  
Decision: **Accept**（真后端 / 记住凭据 Deferred）

## Verification

```text
npx jest packages/features/auth src/navigation/__tests__/authGuard.test.ts --no-coverage
```

## Checklist evidence

| 项 | 证据 |
|----|------|
| 四路由 | `registerAuthFeature` 注册 Login/Password/OTP/Register |
| 校验 | toast 文案与 Flutter `AuthController` 对齐（见 `authUtils` / slice） |
| session | login thunk → `setUser`；logoutThunk → `signOut` + `clearSession` |
| 隐私 | `AuthPrivacyRow` 勾选门禁 |
| 边界 | 无 `@features/*` |
| AuthDevHome | Removed |

## Interaction script

1. 未登录进需鉴权 Tab → Login  
2. 邮箱登录（mock）→ 进主流程；Mine 退出 → Login 且 session 空  
3. 手机 OTP：`123456`；未勾隐私 → toast  
4. 「我要注册」→ Register  

## Deferred

- `USE_MOCK_AUTH=false` 仍挂 Mock（见 supabase `configureAuthService`）  
- Flutter「记住上次登录凭据」未迁  
