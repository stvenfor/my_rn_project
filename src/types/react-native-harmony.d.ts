/**
 * Augment RN Platform with HarmonyOS.
 * Type aliases cannot be extended via declaration merging, so consumers
 * should compare via `(Platform.OS as string) === 'harmony'` or helpers.
 */
export {};
