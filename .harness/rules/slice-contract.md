# Rule — Slice contract

唯一执行单位：`docs/flutter-to-rn-lego-migration/plans/slices/<id>.md`

实现类必填：`本轮 ONLY` · `不做` · `验收` · `文件白名单` · `验证命令` · `证据`  
审计类（ONLY 含只读/audit/不改代码/差距表）：白名单/验证命令可放宽  

子列表项必须缩进。模板：`plans/slices/_template.md`  
机检：`npm run agent:pre` → `check-brief`
